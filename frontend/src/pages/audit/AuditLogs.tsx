import { useEffect, useState } from "react";
import { Alert, Box, Chip, CircularProgress, MenuItem, Paper, Stack, Table, TableBody, TableCell, TableHead, TableRow, TextField, Typography } from "@mui/material";
import { useStore } from "../../contexts/StoreContext"; import api from "../../services/api";
interface Log { id:number; username:string; store_name:string; action:string; action_label:string; model_name:string; object_id:number|null; description:string; created_at:string }
export default function AuditLogs(){const{activeStore}=useStore();const[rows,setRows]=useState<Log[]>([]);const[action,setAction]=useState("");const[loading,setLoading]=useState(false);const[error,setError]=useState("");useEffect(()=>{if(!activeStore)return;setLoading(true);void api.get<Log[]>('/audit-logs/',{params:{store:activeStore.id,action:action||undefined}}).then(r=>setRows(Array.isArray(r.data)?r.data:(r.data as any).results??[])).catch(()=>setError('خطا در دریافت گزارش فعالیت‌ها')).finally(()=>setLoading(false))},[activeStore?.id,action]);return <Box dir="rtl"><Typography variant="h4" sx={{fontWeight:800,mb:.5}}>گزارش فعالیت کاربران</Typography><Typography color="text.secondary" sx={{mb:3}}>ثبت عملیات حساس و قابل پیگیری</Typography>{error&&<Alert severity="error" sx={{mb:2}}>{error}</Alert>}<Paper sx={{p:2,borderRadius:3}}><Stack direction={{xs:'column',md:'row'}} spacing={2} sx={{mb:2}}><TextField select size="small" label="نوع عملیات" value={action} onChange={e=>setAction(e.target.value)} sx={{minWidth:180}}><MenuItem value="">همه</MenuItem>{['create','adjust','payment','cancel','close'].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></Stack>{loading?<Box sx={{display:'grid',placeItems:'center',p:6}}><CircularProgress/></Box>:<Table size="small"><TableHead><TableRow><TableCell>زمان</TableCell><TableCell>کاربر</TableCell><TableCell>عملیات</TableCell><TableCell>بخش</TableCell><TableCell>شرح</TableCell></TableRow></TableHead><TableBody>{rows.map(r=><TableRow key={r.id} hover><TableCell>{r.created_at}</TableCell><TableCell>{r.username}</TableCell><TableCell><Chip size="small" label={r.action_label||r.action}/></TableCell><TableCell>{r.model_name} {r.object_id?`#${r.object_id}`:''}</TableCell><TableCell>{r.description}</TableCell></TableRow>)}{!rows.length&&<TableRow><TableCell colSpan={5} align="center">موردی ثبت نشده است.</TableCell></TableRow>}</TableBody></Table>}</Paper></Box>}
