import { useEffect,useState } from "react";
import { Alert,Box,Button,Card,CardContent,Chip,CircularProgress,Dialog,DialogActions,DialogContent,DialogTitle,Stack,Table,TableBody,TableCell,TableContainer,TableHead,TableRow,TextField,Typography } from "@mui/material";
import EditIcon from "@mui/icons-material/Edit"; import HistoryIcon from "@mui/icons-material/History";
import { useStore } from "../../contexts/StoreContext"; import { listInventory,listInventoryTransactions,updateInventoryMinimum,adjustInventory,type InventoryItem,type InventoryTransaction } from "../../services/inventoryService";
const err=(e:any)=>e?.response?.data?.detail||e?.response?.data?.message||"عملیات انجام نشد.";
export default function Inventory(){const{activeStore,activeRole}=useStore();const[items,setItems]=useState<InventoryItem[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState(""),[edit,setEdit]=useState<InventoryItem|null>(null),[min,setMin]=useState(""),[qty,setQty]=useState(""),[desc,setDesc]=useState(""),[history,setHistory]=useState<InventoryTransaction[]>([]),[historyTitle,setHistoryTitle]=useState("");const load=async()=>{if(!activeStore)return;setLoading(true);try{setItems(await listInventory(activeStore.id))}catch(e){setError(err(e))}finally{setLoading(false)}};useEffect(()=>{void load()},[activeStore?.id]);const saveMin=async()=>{if(!edit||!activeStore)return;try{await updateInventoryMinimum(edit.id,activeStore.id,min);setEdit(null);await load()}catch(e){setError(err(e))}};const adjust=async()=>{if(!edit||!qty)return;try{await adjustInventory(edit.id,qty,desc);setEdit(null);setQty("");setDesc("");await load()}catch(e){setError(err(e))}};const showHistory=async(i:InventoryItem)=>{if(!activeStore)return;try{setHistory(await listInventoryTransactions(activeStore.id,i.product));setHistoryTitle(i.product_name)}catch(e){setError(err(e))}};if(!activeStore)return <Alert severity="warning">ابتدا فروشگاه را انتخاب کنید.</Alert>;const low=items.filter(i=>Number(i.quantity)<=Number(i.min_quantity)).length;return <Box dir="rtl"><Stack direction={{xs:"column",sm:"row"}} sx={{mb:2,justifyContent:"space-between",alignItems:"center"}}><Box><Typography variant="h5" sx={{fontWeight:700}}>مدیریت حرفه‌ای موجودی</Typography><Typography color="text.secondary">موجودی، حداقل سفارش و گردش کالا در فروشگاه {activeStore.name}</Typography></Box><Chip color={low?"warning":"success"} label={`${low} کالای نیازمند تأمین`}/></Stack>{error&&<Alert severity="error" sx={{mb:2}}>{error}</Alert>}{loading?<CircularProgress/>:<Card><CardContent><TableContainer><Table><TableHead><TableRow><TableCell>کالا</TableCell><TableCell>بارکد</TableCell><TableCell>موجودی</TableCell><TableCell>حداقل</TableCell><TableCell>وضعیت</TableCell><TableCell>عملیات</TableCell></TableRow></TableHead><TableBody>{items.map(i=>{const isLow=Number(i.quantity)<=Number(i.min_quantity);return <TableRow key={i.id} hover><TableCell sx={{fontWeight:600}}>{i.product_name}</TableCell><TableCell>{i.barcode||"-"}</TableCell><TableCell>{i.quantity}</TableCell><TableCell>{i.min_quantity}</TableCell><TableCell><Chip size="small" color={isLow?"warning":"success"} label={isLow?"نیازمند تأمین":"مناسب"}/></TableCell><TableCell><Button size="small" startIcon={<HistoryIcon/>} onClick={()=>void showHistory(i)}>گردش</Button>{activeRole&&(activeRole==="manager"||activeRole==="warehouse")&&<Button size="small" startIcon={<EditIcon/>} onClick={()=>{setEdit(i);setMin(i.min_quantity);setQty(i.quantity);setDesc("")}}>تعدیل</Button>}</TableCell></TableRow>})}{!items.length&&<TableRow><TableCell colSpan={6} align="center">موجودی‌ای ثبت نشده است.</TableCell></TableRow>}</TableBody></Table></TableContainer></CardContent></Card>}
<Dialog open={!!edit} onClose={()=>setEdit(null)} fullWidth maxWidth="sm"><DialogTitle>مدیریت موجودی {edit?.product_name}</DialogTitle><DialogContent><Stack spacing={2} sx={{mt:1}}><TextField label="حداقل موجودی" type="number" value={min} onChange={e=>setMin(e.target.value)}/><TextField label="موجودی جدید" type="number" value={qty} onChange={e=>setQty(e.target.value)} helperText="ثبت این مقدار به‌عنوان تعدیل موجودی در گردش کالا ثبت می‌شود."/><TextField label="شرح تعدیل" value={desc} onChange={e=>setDesc(e.target.value)}/></Stack></DialogContent><DialogActions><Button onClick={()=>setEdit(null)}>انصراف</Button><Button onClick={()=>void saveMin()}>ذخیره حداقل</Button><Button variant="contained" onClick={()=>void adjust()}>ثبت تعدیل</Button></DialogActions></Dialog>
<Dialog open={!!historyTitle} onClose={()=>setHistoryTitle("")} fullWidth maxWidth="md"><DialogTitle>گردش {historyTitle}</DialogTitle><DialogContent><Table size="small"><TableHead><TableRow><TableCell>نوع</TableCell><TableCell>مقدار</TableCell><TableCell>شرح</TableCell><TableCell>تاریخ</TableCell></TableRow></TableHead><TableBody>{history.map(t=><TableRow key={t.id}><TableCell>{t.transaction_type==="sale"?"فروش":t.transaction_type==="purchase"?"خرید":t.transaction_type==="return"?"برگشت":"تعدیل"}</TableCell><TableCell>{t.quantity}</TableCell><TableCell>{t.description||"-"}</TableCell><TableCell>{t.created_at}</TableCell></TableRow>)}</TableBody></Table></DialogContent></Dialog></Box>}
