# برنامه فریز‌شده حسابداری ShopProject

این برنامه مبنای توسعه حسابداری است و در میانه کار فاز جدیدی به آن اضافه نمی‌شود. پیاده‌سازی Backend این بسته شامل هسته، یکپارچگی، گزارش‌ها و تست‌های همین برنامه است؛ Frontend بعد از نهایی‌شدن Backend و تست کامل آن ساخته می‌شود.

## فاز 1 — هسته و ایزولاسیون
- App مستقل `backend/accounting`
- Chart of Accounts برای هر فروشگاه
- حساب‌های دارایی، بدهی، سرمایه، درآمد و هزینه
- کد حساب یکتا در هر فروشگاه
- Store-scoped شدن تمام داده‌ها
- دسترسی حسابداری فقط برای مدیر فروشگاه/سوپریوزر
- دوره مالی با بازه تاریخ و وضعیت باز/بسته
- جلوگیری از حذف فروشگاه دارای سابقه حسابداری

## فاز 2 — دفتر روزنامه و کنترل سند
- `JournalEntry` و `JournalLine`
- شماره سند مستقل در هر فروشگاه
- الزام توازن بدهکار/بستانکار
- جلوگیری از ثبت روی حساب گروهی
- جلوگیری از ثبت در دوره بسته
- سندهای ثبت قطعی غیرقابل ویرایش و حذف
- برگشت سند با ساخت سند معکوس
- کلید مرجع یکتا برای جلوگیری از ثبت تکراری

## فاز 3 — یکپارچگی با Backend فعلی، بدون شکستن آن
- فروش و بهای تمام‌شده FIFO از OrderItem
- خرید دریافت‌شده و بدهی تأمین‌کننده
- دریافت از مشتری
- پرداخت به تأمین‌کننده
- هزینه و صندوق
- انتقال بین صندوق‌ها
- تعدیلات دستی صندوق
- مسیرهای اتصال در App مستقل و idempotent
- `ACCOUNTING_AUTO_POSTING=False` به‌عنوان حالت امن پیش‌فرض
- فرمان `sync_accounting` برای همگام‌سازی سوابق قبلی بدون تغییر مالکیت داده‌های ماژول‌های قدیمی

## فاز 4 — گزارش‌های حسابداری
- دفتر کل
- تراز آزمایشی
- گردش حساب
- سود و زیان
- ترازنامه
- کنترل توازن دارایی = بدهی + سرمایه
- فیلتر بر مبنای فروشگاه و تاریخ

## فاز 5 — تست نهایی Backend و فریز
- تست مدل‌ها و محدودیت‌های دیتابیس
- تست توازن اسناد
- تست دوره بسته
- تست برگشت سند
- تست idempotency
- تست ایزولاسیون فروشگاه
- تست دسترسی نقش‌ها
- تست اتصال فروش/خرید/مشتری/تأمین‌کننده/صندوق
- تست گزارش‌ها
- تست حالت `ACCOUNTING_AUTO_POSTING=False`
- یک اجرای کامل تست Backend پس از استقرار این بسته

پس از فاز 5، Backend حسابداری فریز می‌شود و تست جدید طراحی نمی‌شود مگر برای یک Bug واقعی.

## فاز 6 — Frontend حسابداری
این فاز عمداً بعد از فریز Backend انجام می‌شود:
- منوی مستقل «حسابداری» برای فروشگاه فعال
- عدم شلوغ‌کردن منوی اصلی فروشگاه
- محیط مستقل حسابداری
- داشبورد
- حساب‌ها
- دوره مالی
- ثبت سند
- دفتر کل
- تراز آزمایشی
- سود و زیان
- ترازنامه
- گزارش گردش مشتری/تأمین‌کننده
- تاریخ‌های شمسی و UI فارسی

بعد از فاز 6 وارد تست دستی Frontend می‌شویم.
